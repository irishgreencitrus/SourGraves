package io.github.irishgreencitrus.sourgraves.storage

// TODO: add this
//  apparently SQLite is included with Paper, so there's no reason not to use it
//  Make this the primary storage used going forward.
//  We need to detect if we already have a `graves.json` and then convert it to SQLite.
//  Then we switch over.
class SQLiteStorage {
}